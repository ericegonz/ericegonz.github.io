import { Box, Typography } from "@mui/material";
import { contact } from "../../content/contact.data";

const Contact = () => (
  <Box>
    <Typography>{contact}</Typography>
  </Box>
);
export default Contact;
